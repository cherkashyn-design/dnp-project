# Telegram notify for /api/contact
# 1. Message @BotFather → /newbot → copy the bot token
# 2. Start a chat with your bot (or add it to a private channel/group)
# 3. Open https://api.telegram.org/bot<TOKEN>/getUpdates and copy your chat.id
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
